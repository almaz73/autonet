В папке docs_test используется Playwright для тестирования. Вот как запустить тесты:

Перейдите в папку тестов:
Установите зависимости (если ещё не установлены):
Запустите все тесты:
Другие варианты запуска:

npx playwright test --ui — запустить с UI интерфейсом (интерактивно)
npx playwright test --debug — с отладкой
npx playwright test e2e/Обновляемый сайт.spec.ts — запустить конкретный тест файл
npx playwright test --headed — запустить в видимом браузере (не headless)
Рекомендую также добавить скрипты в package.json для удобства.